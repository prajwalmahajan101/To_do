<%if (rtasks.length>0){%>

    <div class="task_list">
        <% for (task of rtasks) {%>
            <div class="task" style="background-color: <%= task.color%>">
                <%= task.content %>
            </div>
            <% } %>
    </div>

    <% } %>